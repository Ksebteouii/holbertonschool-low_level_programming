#include <stdio.h>
#include "main.h"
#include "string.h"

/**
 * rev_string - Reverses a string
 * description :
 * @s: The string to be modified
 * Return: void
 */
int main(void) int i;
char word;
char input;

for (i = 0; i < strlen(input); i += 2)
{
	{
		word[i] += input[i];
	}
	printf("\n\n2nd option\n%s", s);
}
